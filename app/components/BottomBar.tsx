import { View } from "react-native";

export const BottomBar = () => {
  return (
    <View className="flex flex-row justify-between items-center bg-white shadow-lg">
      {/* navigation buttons */}
    </View>
  );
};
